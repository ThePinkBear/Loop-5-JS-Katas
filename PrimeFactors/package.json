{
  "devDependencies": {
    "chai": "^4.3.6",
    "eslint": "^8.17.0",
    "mocha": "^10.0.0"
  },
  "scripts": {
    "lint": "eslint '**/*.js'",
    "lint-watch": "nodemon --exec 'npm run lint --silent'",
    "test": "mocha --exit './{,!(node_modules)/**}/*.spec.js' -w"
  }
}
